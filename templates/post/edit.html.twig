{% extends 'base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}
    <section class="page-card">
        <h1 class="page-title">Editar post</h1>

        {{ include('post/_form.html.twig', {'button_label': 'Actualizar'}) }}

        <div class="mt-2" style="display:flex; justify-content:space-between; align-items:center;">
            <a class="link-muted" href="{{ path('app_post_index') }}">Volver al feed</a>
            {{ include('post/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
